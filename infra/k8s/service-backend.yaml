apiVersion: v1
kind: Service
metadata:
  name: swarmos-backend
  namespace: swarmos
  labels:
    app.kubernetes.io/name: swarmos
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: swarmos
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8765
      targetPort: http
      protocol: TCP

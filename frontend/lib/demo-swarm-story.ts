/**
 * Synthetic Take C augmentation has been retired.
 *
 * Take C is now generated from the causal simulator runtime and consumed by
 * `lib/causal-take-c.ts`. This module intentionally exports no augmentation
 * function so presentation code cannot synthesize support executors, geometry,
 * mission progress, reinforcement composition or physical motion.
 */
export {};

/**
 * Footer — the Control spread's bilingual cadence (spread 24 foot-bar).
 * Editorial serif, mist-mid color, hairline gunmetal border above.
 */
export function Footer() {
  return (
    <div className="bg-absolute-black border-t border-gunmetal px-4 flex items-center justify-between h-[28px]">
      <span className="font-editorial text-eyebrow text-muted-silver">
        One map. One intention.
      </span>
      <span className="font-editorial text-eyebrow text-muted-silver">
        Una mappa. Una sola intenzione.
      </span>
    </div>
  );
}
